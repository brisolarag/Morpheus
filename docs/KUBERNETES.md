# Kubernetes Deployment

If you wish to deploy this architecture into a Kubernetes cluster (as illustrated in the architecture diagram), ensure you have `kubectl` configured, a running cluster (e.g., Minikube, kind, or a cloud provider), and apply your manifest files located in the `infra` folder.

*(Note: The manifests and charts will be fully documented here when completely configured in the repository).*
